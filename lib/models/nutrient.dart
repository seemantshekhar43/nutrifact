import 'package:flutter/material.dart';

class Nutrient{
  String name;
  double value;
  double percentage;
  String unit;
  Color color;

  Nutrient({this.name, this.value, this.percentage, this.unit, this.color});
}